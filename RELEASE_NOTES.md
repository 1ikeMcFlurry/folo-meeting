# Folo Meeting v0.1.0 · 会议与声纹试用版

首个固件与 Android App 配套发布版本。面向已有兼容 8 MB ESP32-C3 工牌硬件，录音器通过 Wi-Fi 直接上传听悟，会后在 App 中编辑纪要、确认声纹候选并归档到飞书。

## 主要功能

- 乐鑫官方 BLE 配网：网络扫描、隐藏网络、逐步连接状态与取消恢复。
- FoloToy 品牌主题、统一页面返回层级、未保存设置提示。
- 听悟实时录音、发言人分离、AI 纪要与会后结果同步。
- 原稿与编辑稿分离，标题、纪要、逐字稿与本场姓名可编辑。
- 讯飞声纹注册、会后片段匹配、低相似度候选人工确认。
- 现有声纹合并补录，并排除训练片段后独立验证。
- 飞书排版预览、创建 / 更新、回读核对与冲突保护。
- 不完整云任务的失败恢复与设备重启诊断信息。

## 下载与升级

| 文件 | 操作 |
| --- | --- |
| `folo-meeting-v0.1.0-android.apk` | Android 8.0+；当前试用签名支持同签名覆盖安装 |
| `folo-meeting-v0.1.0-esp32c3-app.bin` | 仅写入 `0x10000` 应用分区，适用于兼容布局的 8 MB ESP32-C3 |
| `SHA256SUMS.txt` | 下载后校验 SHA-256 |
| `release-manifest.json` | 核对源码提交、签名指纹、文件大小和哈希 |

这是应用升级固件，不是空白芯片全量镜像。不要擦除整片 Flash；保留设备身份、网络与记录。安装包不含测试账号，听悟、讯飞、飞书需要用户自行配置。

完整步骤：[安装与烧录](https://github.com/1ikeMcFlurry/folo-meeting/blob/v0.1.0/doc/meeting/INSTALL.md) · [使用说明](https://github.com/1ikeMcFlurry/folo-meeting/blob/v0.1.0/README.md)

## 验证情况

- 整理后的仓库重新构建 Android APK 与 ESP32-C3 固件。
- 60 项 JVM 单元测试通过，Android lint 无错误。
- 功能开发阶段已验证官方 BLE 配网、19 秒真人录音、声纹补录 / 独立匹配、确认姓名后的保存和文档预览。
- 真机音频解码测试使用合成音频通过，覆盖多次 seek、重采样与时间定位。

## 已知限制

- 仍是试用版本；长会议稳定性和多人声纹准确率未完成验收。
- 声纹候选全部默认不勾选，需人工确认；相似度不是成功率。
- 开启会后音频输出只影响新会议，旧会议缺少音频时无法补做声纹识别。
- 设备不保存会议音频，断网时不承诺离线补传；云端音频保留策略由听悟管理，App 暂不能代删。
- 曾反馈一次开始录音后的设备重启，后续短录音未复现，根因尚未确认。
- APK 沿用当前测试签名，尚未配置正式商店分发签名。

本次发布重新构建了二进制，固件版本元数据统一为 0.1.0；没有再次刷机录音。真实硬件功能验证来自发布前同一功能实现的测试记录。
